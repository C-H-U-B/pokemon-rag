[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pokemon-rag"
version = "0.1.0"
description = "Local hybrid RAG and structured query system for Pokémon data"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]